// Stacks as Array => Optimal; push() and pop() keep it O(1)
class Stack {
    constructor() {
        this.data = [];
    }

    // Methods
}

// Stacks as Linked List / Doubly Linked List => Optimal; adding and removing O(1)