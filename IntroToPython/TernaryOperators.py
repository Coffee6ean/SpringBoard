# Ternary -
# JavaScript:
#   -> let msg = (age>=18) ? "Vote" : "Can't"
# Python:
#   -> msg = "Vote" if (age >= 18) else "Can't"
color = "teal"
print("good choice") if color == "teal" else print("Nah")

# -> do_if_true if CONDITION else do_if_false
# -> condition ? do_if_true : do_if_false